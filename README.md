# get_func_definition
A small tool to get which file target func's definition is located at (In IoT Firmware's filesystem).

## Usage
```shell
usage: get_func_definition.py [-h] -f FILESYSTEM -n FILENAME -t FUNCTION
```
```
